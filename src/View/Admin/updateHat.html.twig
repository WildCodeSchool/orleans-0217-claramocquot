{% extends "homeAdmin.html.twig" %}

{% block content %}

    <div class="container text-center">
        <h1>Modifier un article :</h1><hr/>
        <a href="index.php?route=chapeaux"><p class="text-left"><span class="fa fa-angle-double-left"></span> retour à la
                liste</p></a><br/>
        {% if result is not empty %}
            <blockquote class="btn-success">{{ result }}</blockquote><br/>
        {% endif %}
        {% if noResult is not empty %}
            <blockquote class="btn-danger">{{ noResult }}</blockquote><br/>
        {% endif %}
        {{ form.render|raw }}
    </div>
{% endblock %}